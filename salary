%facts
salary(vaishnavi,35000).
salary(pranali,65000).
salary(sakshi,80000).

%rules
low_salary(X):-
      salary(X,S),
      S<40.

medium_salary(X):-
      salary(X,S),
      S>=40.

high_salary(X):-
      salary(X,S),
      S>=70.



